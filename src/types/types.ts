export type Movie ={
  titulo: string;
  avatar: string;
}

export type Posts = {
  userID: number;
  id: number;
  title: string;
  body: string;
}
export type PokemonTypes = {
  count: number
  next: null,
  previous: null,
  results: []
}